import { Typography } from "@mui/material";

const CheckoutPage = () => {
    return ( 
        <Typography>
            Only authorized user should be able to see this
        </Typography>
    );
}

export default CheckoutPage;